# Password for the theme studio at /themes.
# Copy this file to .env.local and set your own value; the endpoint refuses all
# requests until you do.
#
# The deployed studio needs the same variable set in the Vercel project, plus
# THEME_STUDIO_GH_TOKEN so it can commit. See docs/theme-studio.md.
THEME_STUDIO_PASSWORD=
